.venv/
.venv-analysis/
__pycache__/
*.pyc
*.sqlite
*.db
.DS_Store
*.log

# Secrets: completion codes, API tokens, database URLs
.env

# psiTurk leftovers
.psiturk_history

# Participant data exports carry real Prolific IDs and this repository is
# public. Never commit them. The five CSVs already tracked here predate this
# rule and contain only debug runs; git keeps tracking them regardless.
db_exports/*.csv
